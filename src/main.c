#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>

#include "vec.h"
#include "objgen/objgen.h"

int main() {
    create_obj();

    // uint8_t* nums = NULL;
    // for (size_t i = 0; i < 3; i++) {
    //     vec_push(nums, i);
    // }
    // for (size_t i = 0; i < vec_len(nums); i++) {
    //     printf("%d, ", nums[i]);
    // }
    // printf("\n");
    // vec_free(nums);
    return 0;
}